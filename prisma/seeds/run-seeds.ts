import prisma from '@/lib/prisma';
import generateSeeds from './seeds';

generateSeeds()
  .then(async () => {
    await prisma.$disconnect();
  })
  .catch(async (e) => {
    console.error(e);
    await prisma.$disconnect();
    process.exit(1);
  });
